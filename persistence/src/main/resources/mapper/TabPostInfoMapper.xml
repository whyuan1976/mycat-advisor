<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="org.apache.mycat.advisor.persistence.dao.TabPostInfoMapper">

        <select id="getIdAndNameByName" resultType="map">
                <![CDATA[
                SELECT id,name FROM tab_post_info where name like #{name}
                ]]>
        </select>

</mapper>